<% provide(:title, '登録している人間 or ねこ') %>
<div class="container-fluid">
  <div class="jumbotron_4">
    <div class="bg-mask">
      <div class="row">
        <div class="col-md-12">
          <h3>登録している人間 or ねこ</h3>
          <%= will_paginate %>
          <ul class="users">
            <%= render @users %>
          </ul>
          <%= will_paginate %>
        </div>
      </div>
    </div>
  </div>
</div>